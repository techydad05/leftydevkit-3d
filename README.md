# LeftyDevKit 3D

Single-file Three.js landing site with a particle-fist hero (scatter → fist gather, holds).
Served as static files via nginx on Coolify.

Local preview:

    python3 -m http.server 8020 --bind 127.0.0.1

Then open http://127.0.0.1:8020/
